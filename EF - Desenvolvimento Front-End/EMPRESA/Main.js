class Main {
    static iniciar() {
        try {
            // ... criação de objetos e chamadas de métodos ...
        } catch (error) {
            console.error("Erro:", error.message);
        }
    }
}

Main.iniciar();